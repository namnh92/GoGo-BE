# ADM-019 — canonical profile area

Refs #566. Stacked on #577; merge/rebase that parent first. OpenAPI alpha.31.

PATCH /me accepts homeAdministrativeArea with datasetVersion, provinceCode and optional nullable communeCode. Whole-province scope is supported; a place address still requires a commune. The published dataset is locked while validating and writing. The server supplies display labels, rejects wrong hierarchy and stale versions, and returns needs_reselection with saved labels for historical selections.

Migration 0062 adds a nullable snapshot column. Existing profiles retain homeArea; no guessed backfill. An explicit canonical write clears the legacy selection, and a legacy-client write clears the canonical selection. Omitted fields remain unchanged. The area is private to the profile API, included in the existing authenticated export, erased on account deletion, and excluded from audit values.

Validation: 31 PostgreSQL profile integration tests and 1,587 unit tests pass. Typecheck, lint, OpenAPI generation and package boundaries pass. The integration suite applies migrations to an empty isolated PostGIS database and covers set/read/clear, whole province, invalid hierarchy/version, historical labels, legacy compatibility, export and deletion. DEV/device acceptance is pending.

Rollback: revert the application consumer first and retain the additive nullable column. No destructive backfill occurs. Dropping the column is only for an isolated migration rehearsal; do not erase saved selections during application rollback.
